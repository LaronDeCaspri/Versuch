# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# android/ wird mitgeliefert, damit die Standort-Berechtigungen (GPS) erhalten
# bleiben. Nur maschinenspezifische / generierte Dateien ausschließen:
/android/local.properties
/android/.gradle/
/android/app/build/
*.iml

# Die übrigen Plattform-Ordner lokal mit `flutter create .` erzeugen:
/ios/
/linux/
/macos/
/windows/
/web/

# IDE
.idea/
*.iml
.vscode/

# HERE-Zugangsdaten NIEMALS committen
**/here_credentials.dart
.env
